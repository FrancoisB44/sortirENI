{% extends ('base.html.twig')%}

{% block container %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button> Valider</button>
    <button> Annuler</button>
    {{ form_end(form) }}
{% endblock %}